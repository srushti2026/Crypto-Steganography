{
  "name": "veilforge-steganography",
  "version": "1.0.0",
  "description": "VeilForge Steganography Application with React Frontend and FastAPI Backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview"
  },
  "workspaces": [
    "frontend"
  ],
  "dependencies": {
    "@emailjs/browser": "^4.4.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
